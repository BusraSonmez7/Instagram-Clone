import {StyleSheet} from 'react-native';
import {colors} from '../../../../../color';
export default StyleSheet.create({
  storyContainer: {
    alignItems: 'center',
  },
  storyName: {
    color: colors.black,
    fontSize: 11,
    width: 80,
    textAlign: 'center',
  },
});
